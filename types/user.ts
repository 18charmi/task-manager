export type User = {
  email: string;
  password: string;
};

export type LoginForm = {
    username: string
    password: string
}
export type RegisterForm = {
    username: string
    password: string
}